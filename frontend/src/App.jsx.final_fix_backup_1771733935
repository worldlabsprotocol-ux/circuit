import React, { useState } from 'react';
import { Buffer } from 'buffer';
window.Buffer = Buffer; // Polyfill for Solana buffer issues

// Page imports (add more if needed)
import Home from './pages/Home';
import Create from './pages/Create';
import Mic from './pages/Mic';
import Royalties from './pages/Royalties';
import Mix from './pages/Mix';

function App() {
  const [activeTab, setActiveTab] = useState('Create');

  return (
    <div className="min-h-screen bg-black text-white flex flex-col relative">
      {/* Starfield background placeholder - add your canvas or CSS here */}
      <div className="absolute inset-0 pointer-events-none bg-[radial-gradient(#000 80%, #0000 100%)] opacity-50" />

      {/* Header with neon wave and wallet */}
      <header className="p-4 text-center">
        <h1 className="text-5xl font-bold text-cyan-400 drop-shadow-[0_0_10px_cyan]">CIRCUIT</h1>
        {/* Neon wave placeholder - replace with your SVG or component */}
        <div className="mt-4 h-1 bg-gradient-to-r from-cyan-400 to-cyan-600 shadow-[0_0_20px_cyan]" />
        <button className="mt-4 px-6 py-2 bg-purple-600 rounded-full text-white hover:bg-purple-500 transition">Select Wallet</button>
      </header>

      {/* Main content - tab switching */}
      <main className="flex-1 p-4 overflow-y-auto pb-24">
        {activeTab === 'Home' && <Home />}
        {activeTab === 'Create' && <Create />}
        {activeTab === 'Mic' && <Mic />}
        {activeTab === 'Royalties' && <Royalties />}
        {activeTab === 'Mix' && <Mix />}
      </main>

      {/* Bottom glowing navigation */}
      <nav className="fixed bottom-0 left-0 right-0 bg-black/70 backdrop-blur-md border-t border-cyan-400/30 z-50">
        <div className="flex justify-around py-3 max-w-md mx-auto">
          {['Home', 'Create', 'Mic', 'Royalties', 'Mix'].map((tab) => (
            <button
              key={tab}
              onClick={() => setActiveTab(tab)}
              className={\`px-4 py-2 rounded-full text-sm font-medium transition-all \${activeTab === tab ? 'bg-cyan-400/30 text-cyan-200 shadow-md shadow-cyan-400/50 scale-105' : 'text-cyan-400/70 hover:text-cyan-200'}\`}
            >
              {tab}
            </button>
          ))}
        </div>
      </nav>
    </div>
  );
}

export default App;
